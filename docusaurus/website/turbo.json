{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".docusaurus/**", "build/**"],
      "cache": false
    },
    "build:dev": {
      "dependsOn": ["^build"],
      "outputs": [".docusaurus/**", "build/**"],
      "cache": false
    }
  }
}
